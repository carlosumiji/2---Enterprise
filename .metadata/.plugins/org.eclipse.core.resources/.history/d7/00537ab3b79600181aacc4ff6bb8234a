package br.com.fiap.spring.controller;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("carro")
public class CarroController {

	//Método que abre a página com o formulário
	@GetMapping("cadastro")
	public String abrirHome() {
		return "carro/cadastro"; 
	}
	
	//Metodo que Processa as infomação do formulário
	@PostMapping("cadastro")
	public String processarForm(String placa, String preco, boolean automatico, String marcas){
		System.out.println( + " " + telefone + " " + email);
		return "carro/cadastro";
	}
}
